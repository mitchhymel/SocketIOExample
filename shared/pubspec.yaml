name: shared
description: A new Flutter package project.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  enum_to_string:
  json_annotation:

dev_dependencies:
  flutter_lints: ^1.0.0
  build_runner:
  json_serializable:

